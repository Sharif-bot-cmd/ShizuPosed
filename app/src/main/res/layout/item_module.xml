<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardModule"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Top row: Icon, Name, Switch -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- App Icon -->
            <ImageView
                android:id="@+id/ivIcon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_module"
                android:contentDescription="@string/module_icon"
                android:layout_marginEnd="12dp" />

            <!-- Module Info -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Module Name"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/tvPackage"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="com.example.module"
                    android:textSize="12sp"
                    android:textColor="?android:attr/textColorSecondary"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/tvVersion"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="v1.0"
                        android:textSize="12sp"
                        android:textColor="?android:attr/textColorSecondary" />

                    <TextView
                        android:id="@+id/tvEntry"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Entry: MainHook"
                        android:textSize="11sp"
                        android:textColor="?android:attr/textColorSecondary"
                        android:layout_marginStart="8dp"
                        android:maxLines="1"
                        android:ellipsize="middle" />

                </LinearLayout>

            </LinearLayout>

            <!-- Enable Switch -->
            <com.google.android.material.materialswitch.MaterialSwitch
                android:id="@+id/swEnabled"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp" />

        </LinearLayout>

        <!--
            Bottom row: Hooked Apps Info + Recommended hint + Select Apps.

            ── CHANGE: added tvRecommended between the hooked-apps
            count and the Select Apps button.

            Layout behavior:
              • tvHookedApps keeps weight=1, so it absorbs the
                remaining space and stays left-aligned.
              • tvRecommended sits immediately to its right with a
                small start margin. It is GONE by default, so when
                the module declares no scope.list the row looks
                exactly as before — no extra gap, no phantom space.
              • btnSelectApps stays pinned to the trailing edge.
        -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvHookedApps"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Hooked Apps: 0"
                android:textSize="12sp"
                android:textColor="?android:attr/textColorSecondary" />

            <!--
                ── CHANGE: recommended count hint.
                Visibility is driven entirely by ModuleAdapter.
                GONE when the module declares no scope.list, so this
                view is invisible in the common case.
            -->
            <TextView
                android:id="@+id/tvRecommended"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:text="· 0 recommended"
                android:textSize="12sp"
                android:textColor="?attr/colorPrimary"
                android:maxLines="1"
                android:visibility="gone" />

            <Button
                android:id="@+id/btnSelectApps"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select Apps"
                android:textSize="11sp"
                android:padding="4dp"
                android:minWidth="0dp"
                android:minHeight="0dp"
                style="@style/Widget.AppCompat.Button.Borderless" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>