<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - Auto-fit and modern -->
    <style name="Theme.ShizuPosedManager" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorPrimaryDark">@color/md_theme_light_primaryContainer</item>
        <item name="colorAccent">@color/md_theme_light_secondary</item>

        <!-- Surface & Background -->
        <item name="android:windowBackground">@color/md_theme_light_surface</item>
        <item name="android:colorBackground">@color/md_theme_light_surface</item>

        <!-- Auto-fit: Make content fill the screen -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/md_theme_light_surface</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <!-- Window animations -->
        <item name="android:windowAnimationStyle">@style/Animation.Activity</item>
    </style>

    <!-- Toolbar theme: renamed so it no longer extends itself.
         Reference this from your Toolbar/theme overlay usage as
         @style/ThemeOverlay.ShizuPosedManager.ActionBar -->
    <style name="ThemeOverlay.ShizuPosedManager.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <!-- Activity animation -->
    <style name="Animation.Activity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@android:anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@android:anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@android:anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>